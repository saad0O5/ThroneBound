package engine;

/**
 * Lane
 * Owner: Member B (Game Engine)
 *
 * Responsibility:
 *   - Represents one of the 3 board slots per player. Holds at most one Card.
 *   - A card in a lane can only be attacked by (or attack) the card in the
 *     mirrored enemy lane; an empty mirrored lane means a hit goes straight
 *     to the opponent's Life Total.
 *
 * TODO:
 *   - [ ] Field: occupant (Card, nullable)
 *   - [ ] Implement isEmpty()
 *   - [ ] Implement placing/removing a card from this lane
 */
public class Lane {
    // TODO: field - Card occupant

    // TODO: public boolean isEmpty()
}
