package gui;

/**
 * LoginScreen
 * Owner: Member C (GUI)
 *
 * Responsibility:
 *   - First screen shown on launch. Username/password fields, Login and
 *     Register buttons/actions, delegating to persistence.ProfileManager.
 *   - On success, navigates to MainMenuScreen.
 *
 * TODO:
 *   - [ ] Build the JavaFX layout (username field, password field, buttons)
 *   - [ ] Wire Login button -> ProfileManager.login(...)
 *   - [ ] Wire Register button -> ProfileManager.register(...)
 *   - [ ] Show a clear error message on failed login/duplicate username
 *   - [ ] On success, transition to MainMenuScreen, passing the logged-in PlayerProfile
 */
public class LoginScreen {
    // TODO
}
