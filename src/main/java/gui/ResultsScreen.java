package gui;

/**
 * ResultsScreen
 * Owner: Member C (GUI)
 *
 * Responsibility:
 *   - Shown after a match ends: win/loss banner, match summary, any newly
 *     unlocked card (see persistence.UnlockManager), then navigation back to
 *     MainMenuScreen.
 *
 * TODO:
 *   - [ ] Build win/loss banner + summary layout
 *   - [ ] Call UnlockManager.checkUnlocks(...) and display any new unlock
 *   - [ ] Append this match to the player's MatchHistory via ProfileManager.save()
 *   - [ ] "Back to Menu" button -> MainMenuScreen
 */
public class ResultsScreen {
    // TODO
}
