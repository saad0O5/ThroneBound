package engine;

/**
 * Player
 * Owner: Member B (Game Engine)
 *
 * Simple enum identifying which side a turn/action belongs to.
 * Fully implemented — no logic to fill in here.
 */
public enum Player {
    PLAYER1,
    PLAYER2
}
