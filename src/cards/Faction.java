package cards;

/**
 * Faction (abstract base class)
 * Owner: Member B
 *
 * Responsibility:
 *   - Represents a playable faction and its available card pool. Three
 *     concrete factions: BeastkinFaction, ArcaneOrderFaction, UndeadLegionFaction.
 *
 * TODO:
 *   - [ ] Field: cardPool (List<Card>) — the 15 faction cards + access to the
 *         shared neutral pool (see docs/Card_List.md)
 *   - [ ] Method to populate cardPool with this faction's specific cards
 */
public abstract class Faction {
    // TODO: field - List<Card> cardPool
}
