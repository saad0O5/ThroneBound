package persistence;

/**
 * UnlockManager
 * Owner: Member C (GUI & Persistence)
 *
 * Responsibility:
 *   - Decides what new cards (if any) a player unlocks after a match, based on
 *     the match result, and updates their PlayerProfile accordingly.
 *
 * TODO:
 *   - [ ] Design the unlock rule (e.g. every N wins unlocks the next card in a
 *         faction's roster) — discuss with team and document the chosen rule
 *   - [ ] Implement checkUnlocks(PlayerProfile profile, matchResult): update
 *         profile.unlockedCards if applicable
 */
public class UnlockManager {
    // TODO: public void checkUnlocks(/* PlayerProfile profile, MatchResult result */)
}
