package persistence;

/**
 * MatchRecord
 * Owner: Member C (GUI & Persistence)
 *
 * Responsibility:
 *   - A single past match's summary: opponent, result (win/loss), date.
 *
 * TODO:
 *   - [ ] Fields: opponent (String), result (String or enum), date (String or
 *         a proper date type)
 *   - [ ] Constructor
 */
public class MatchRecord {
    // TODO: fields - opponent, result, date
}
