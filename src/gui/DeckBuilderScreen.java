package gui;

/**
 * DeckBuilderScreen
 * Owner: Member C (GUI)
 *
 * Responsibility:
 *   - Faction selection, then a 25-card deck-building interface showing only
 *     the player's unlocked cards for that faction (+ neutral pool).
 *
 * TODO:
 *   - [ ] Build faction selection UI (3 large selectable panels per Faction)
 *   - [ ] Build the card grid showing unlocked cards, click/drag to add to deck
 *   - [ ] Show live deck count (must reach exactly 25 to be valid — see Deck.validate())
 *   - [ ] Save the built deck to the PlayerProfile's saved decks
 *   - [ ] "Ready" button, enabled only when Deck.validate() passes
 */
public class DeckBuilderScreen {
    // TODO
}
